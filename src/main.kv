WindowManager:
    id: screen_manager
    MainScreen:
        id: main_screen
        name: "main"
        manager: "screen_manager"
    BoardScreen:
        id: board_screen
        name: "board"
        manager: "screen_manager"
    ScoreScreen:
        id: score_screen
        name: "score"
        manager: "screen_manager"

<Tile>:
    color: 0, 0, 0, 1
    font_size: dp(40)

<WhiteBackground@Widget>:
    canvas.before:
        Color:
            rgba: 1, 1, 1, 1
        Rectangle:
            pos: self.pos
            size: self.size

<BoardScreen>:
    WhiteBackground:

    GridLayout:
        id: layout

<MainScreen>:
    BoxLayout:
        orientation: "vertical"
        BoxLayout:
            orientation: "horizontal"
            Label:
                text: "width"
            TextInput:
                id: width_input
                text: "5"
        BoxLayout:
            orientation: "horizontal"
            Label:
                text: "height"
            TextInput:
                id: height_input
                text: "5"
        BoxLayout:
            orientation: "horizontal"
            Label:
                text: "bomb chance"
            TextInput:
                id: bomb_chance_input
                text: "30"
        Button:
            text: "New Game"
            on_press: app.root.current = "board"; app.root.get_screen("board").setup(int(width_input.text), int(height_input.text), int(bomb_chance_input.text))

<ScoreScreen>:
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "Score"
        Label:
            id: cleared_tiles_label
            text: "Cleared tiles"
        Label:
            id: correctly_guessed_bombs_label
            text: "Correctly guessed bombs"